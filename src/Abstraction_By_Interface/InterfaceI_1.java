package Abstraction_By_Interface;

public interface InterfaceI_1 {
	
	//Multiple Inheritance Support through Interface :
	//Multiple parent class inherit by single child class
	
	void play();
	
	// void play();   *** If we write like this then it does not show error because compile automatically add public abstract in front of void play();

}
